// Немецкий копирайт калькулятора.
// Владение копирайтом объявляет доступность калькулятора в локали: без этого
// файла немецкой страницы не существует. Подробный текст живёт в
// `src/data/deContent/`.

import type { CalculatorSeoCopy } from '../../lib/platform/types';

export const unixTimestampCopyDe: CalculatorSeoCopy = {
  name: 'Umrechner für den Unix-Zeitstempel',
  slug: 'unix-zeitstempel-umrechner',
  shortDescription: 'Zwischen Unix-Zeit und einem UTC-Datum umrechnen, in beide Richtungen.',
  seoTitle: 'Unix-Zeitstempel umrechnen — Epochensekunden in UTC-Datum',
  seoDescription: 'Rechne einen Unix-Zeitstempel in ein UTC-Datum um und zurück, mit Wochentag und unabhängig von deiner Zeitzone.',
  h1: 'Umrechner für den Unix-Zeitstempel',
  keywords: ['Unix-Zeitstempel umrechnen', 'Epochenzeit', 'Zeitstempel in Datum', 'Unix Zeit'],
};
