// Немецкий копирайт калькулятора.
// Владение копирайтом объявляет доступность калькулятора в локали: без этого
// файла немецкой страницы не существует. Подробный текст живёт в
// `src/data/deContent/`.

import type { CalculatorSeoCopy } from '../../lib/platform/types';

export const cssUnitsCopyDe: CalculatorSeoCopy = {
  name: 'Umrechner für CSS-Einheiten',
  slug: 'css-einheiten-umrechner',
  shortDescription: 'px, rem, em, pt und die übrigen CSS-Längeneinheiten umrechnen.',
  seoTitle: 'CSS-Einheiten umrechnen — px, rem, em, pt',
  seoDescription: 'Rechne CSS-Einheiten zwischen px, rem, em, pt, pc, in, cm und mm um, unter Berücksichtigung der Wurzel- und der Elternschriftgröße.',
  h1: 'Umrechner für CSS-Einheiten',
  keywords: ['CSS-Einheiten umrechnen', 'px in rem', 'em oder rem', 'pt umrechnen'],
};
